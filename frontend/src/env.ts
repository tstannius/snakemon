// const env = process.env.APP_ENV;

// let envApiUrl = '';

// if (env === 'PROD') {
//   envApiUrl = `https://${process.env.DOMAIN_PROD}`;
// } else if (env === 'STAG') {
//   envApiUrl = `https://${process.env.DOMAIN_STAG}`;
// } else {
//   envApiUrl = `http://${process.env.DOMAIN_DEV}`;
// }

export const apiUrl = "http://localhost:8000";
